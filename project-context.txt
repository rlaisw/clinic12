### PROJECT ###
Expired: Medication & Patient Management System (Next.js 16 + Django 6 / SQLite)
Working dir: /home/ubuntu/kilocode/clinic12

### PACKAGE.JSON ###
{
  "name": "with-tailwind",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:https": "bash certs/generate.sh && pnpm --filter web dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.6.0",
    "prettier-plugin-tailwindcss": "^0.7.1",
    "turbo": "^2.9.16"
  },
  "packageManager": "pnpm@10.19.0",
  "engines": { "node": ">=18" },
  "dependencies": { "recharts": "^3.8.1" }
}

### FOLDER STRUCTURE (Top 2 Levels) ###
├── README.md
├── TECH_STACK.md
├── DEPLOYMENT_CHECKLIST.md
├── certs/            # self-signed certs + generate.sh
├── .github/workflows/ # ci.yml, deploy.yml
├── apps
│   ├── docs
│   └── web           # Next.js at apps/web, HTTPS server.js
├── backend
│   ├── api
│   ├── config
│   ├── db.sqlite3
│   ├── manage.py
│   ├── requirements.txt
│   └── venv
├── package.json
├── packages
│   ├── eslint-config
│   ├── tailwind-config
│   ├── typescript-config
│   └── ui
├── pnpm-lock.yaml
├── pnpm-workspace.yaml
├── turbo.json
└── .cocoindex_code/  # ccc (cocoindex-code) project index

### NEXT CONFIG ###
apps/web/next.config.ts:
- allowedDevOrigins includes localhost, vps.tailb5775.ts.net (+ port variants)
- rewrites /api/:path* -> https://vps.tailb5775.ts.net:8000/api/:path*
- rewrites /chat/:path* -> https://10.0.1.75/chat/:path*

### HTTPS / PORTS ###
- Frontend :3001 HTTPS (server.js uses certs/clinic.com.hk.{crt,key}) — also the origin proxy
- Backend  :8000 HTTPS (runserver_plus --cert/--key)
- Dify     :80/443 via nginx pinned to 10.0.1.75 (private IP)
- Tailscale Funnel : https://vps.tailb5775.ts.net -> https+insecure://127.0.0.1:3001

### ORIGIN PROXY (apps/web/server.js) ###
- /api/*            -> Django 127.0.0.1:8000
- /dify/* (strip)   -> Dify 10.0.1.75:80
- /chat/* (rewrite) -> Dify 10.0.1.75:80 (HTML: /_next/ -> /dify/_next/, ws://localhost -> /socket.io/)
- /socket.io/*      -> Dify 10.0.1.75:80 (polling HTTP + raw websocket upgrade)
- /assets/ /static/ /fonts/ /images/ -> Dify 10.0.1.75:80
- Dify blocking chat timeout: 420 s

### SERVICES ###
- Start all (fg|background|stop|restart|status): bash start-all.sh
- Foreground only: bash start-dev.sh
- Funnel: tailscale funnel --bg https+insecure://127.0.0.1:3001
- Stop funnel: tailscale funnel reset
- Certs: bash certs/generate.sh

### DIFY & RAG CONFIGURATION ###
**✅ WORKING**
- Dify web UI: https://vps.tailb5775.ts.net/dify/ (via server.js proxy, :3001)
- Hosted chat: https://vps.tailb5775.ts.net/chat/45322G8rzGMEW7WP (via proxy)
- In-app AI chatbot: /doctor/patients/{id}/ai-chatbot -> DifyChat (direct API /dify/api/chat-messages, passport from /dify/api/passport)
- RAG API: https://vps.tailb5775.ts.net:8000/api/rag/query (token-protected)
- Vector store: LanceDB (backend/lancedb/), populated by rag/cocoindex_pipeline.py
- Auth: Django Token Authentication
- SSRF proxy: SSRF_PROXY_HTTP_URL/HTTPS_URL=http://ssrf_proxy:3128
- Sandbox proxy: SANDBOX_HTTP_PROXY/HTTPS_PROXY=http://ssrf_proxy:3128

### CI/CD ###
- .github/workflows/ci.yml — lint, type-check, backend tests, Django check --deploy, bandit (Node 22 + Py 3.12)
- .github/workflows/deploy.yml — build + collectstatic (deploy placeholder)

### CODE SEARCH INDEX ###
- cocoindex-code: `ccc index` / `ccc search <query>` (project DB .cocoindex_code/target_sqlite.db, ~356 files)
- codebase-memory: graph index (~3.8k nodes / ~9.4k edges), auto-refresh

### ENVIRONMENT SETTINGS ###
- backend/.env: DEBUG, SECRET_KEY, DATABASE_URL, HF_TOKEN
- apps/web/.env: NEXT_PUBLIC_API_URL=/api
- CORS: backend/config/settings.py allowed origins include vps.tailb5775.ts.net + dev IPs

### KEY FILES MODIFIED (this session) ###
- apps/web/server.js                 — HTTPS on :3001 + Dify origin proxy (/dify, /chat, /socket.io, assets)
- apps/web/components/doctor/dify-chat.tsx — direct-API Dify chat client (passport, chat-messages, safe table rendering, Shift+Enter = newline)
- apps/web/app/(dashboard)/doctor/patients/[id]/layout.tsx — AI Chatbot tab renders DifyChat
- apps/web/next.config.ts            — allowedDevOrigins for funnel host
- start-all.sh / start-dev.sh        — lifecycle (fg/bg/stop/restart/status), pre-warm, funnel-last
- .github/workflows/ci.yml           — Node 22, max-warnings 999
- README.md, TECH_STACK.md, DEPLOYMENT_CHECKLIST.md, USER_GUIDE.md — refreshed
- certs/ regenerate with vps.tailb5775.ts.net + clinic.com.hk SANs